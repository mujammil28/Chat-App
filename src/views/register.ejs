<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/5.0.0/normalize.min.css">
    <style type="text/css">
        html, body {
            height: 100%;
            margin: 0;
        }

        .left {
            background-color: #00bc7b;
            width: 50%;
            height: 100%;
            float: left;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .right {
            width: 50%;
            height: 100%;
            float: right;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        .register_image {
            max-width: 30vw;
            max-height: 30vh;
            border-radius: 50%;
        }

        .content {
            text-align: center;
            color: white;
            padding: 20px;
        }

        .tagline {
            font-family: Montserrat;
            font-weight: 600;
            font-size: 21px;
            line-height: 25.6px;
        }

        .logo {
            max-width: 20vw;
            max-height: 20vh;
            border-radius: 50%;

        }

        .welcome {
            font-weight: 700;
            font-size: 35px;
            line-height: 1.5;
            margin: 0;
        }

        .welcome1 {
            font-weight: 600;
            font-size: 18px;
            line-height: 1.5;
            margin: 0;
            color: #736F6F;
        }

        /* Form styling */
        .register-form {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 20px;
        }

        .register-input {
            width: 300px;
            padding: 10px;
            margin: 10px;
            border-radius: 20px;
            border: 1px solid #00bc7b;
            box-sizing: border-box;
            font-size: 16px;
        }

        .register-button {
            width: 200px;
            padding: 12px 20px;
            border-radius: 30px;
            background-color: #00bc7b;
            color: #fff;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
            font-weight: bold;
            text-transform: uppercase;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            font-size: 16px;
        }

        .register-button:hover {
            background-color: #28865d;
        }

        .login-link {
            margin-top: 20px;
            font-size: 14px;
            color: #00bc7b;
            text-decoration: none;
        }

        .login-link:hover {
            text-decoration: underline;
        }

        /* Alert styling */
        
        :root {
  --primary-color: #00bc7b;
}

input[type=file] {
  padding: 4px;
  margin: -4px;
  position: relative;
  outline: none;
  /* File Selector Button Styles */
  /* Faked label styles and icon */
  /* Handle Component Focus */
}
input[type=file]::file-selector-button {
  border-radius: 10px;
  padding: 0 16px;
  height: 32px;
  cursor: pointer;
  background-color: white;
  border: 0px solid rgb(255, 250, 250);
  box-shadow: 0px 1px 0px rgba(0, 0, 0, 0.05);
  margin-right: 16px;
  /*
    This is a hack to change the button label. 
    I'm hiding the default label and then 
    manually applying the width based on 
    updated icon and label.
  */
  width: 150px;
  color: transparent;
  /*
    Firefox doesn't support the pseudo ::before 
    or ::after elements on this input field so 
    we need to use the @supports rule to enable 
    default styles fallback for Firefox.
  */
}
@supports (-moz-appearance: none) {
  input[type=file]::file-selector-button {
    color: #00bc7b;
  }
}
input[type=file]::file-selector-button:hover {
  background-color: #ffffff;
}
input[type=file]::file-selector-button:active {
  background-color: #ffffff;
}
input[type=file]::before {
  position: absolute;
  pointer-events: none;
  top: 11px;
  left: 1px;
  height: 20px;
  width: 20px;
  content: "";
 
}
input[type=file]::after {
  position: absolute;
  pointer-events: none;
  top: 13px;
  left: 10px;
  color: #736F6F;
  content: "Upload Image";
}
input[type=file]:focus-within::file-selector-button, input[type=file]:focus::file-selector-button {
  outline: none;
  outline-offset: none;
}


    </style>
</head>
<body>
  
    <div class="left">
        <div class="content">
            <img class="register_image" src="/public/media/logoImg.png" alt="Register Image">
            <p class="tagline">Step into a world where understanding meets action. <br>Get ready to connect with group, meet and chat.</p>
        </div>
    </div>
    <div class="right">
        <img class="logo" src="/public/media/logoImg.png" alt="logo">
        <h3 class="welcome">Welcome Buddy!</h3>
        
        <!-- Registration Form -->
        <form action="/register" method="post" class="register-form" id="register-form" enctype="multipart/form-data">
            <input type="text" name="name" id="name" class="register-input" placeholder="Full Name" required>
            <input type="email" name="email" id="email" class="register-input" placeholder="Email" required>
            
            <div class="custom-uploader">            
                    <input type="file" name="image" id="image" class="register-input" placeholder="Upload Image" accept="images/*" required>
            </div>
    
            <input type="password" name="password" id="password" class="register-input" placeholder="Password" required>
            <button type="submit" class="register-button">Register</button>
        </form>
        <!-- Login Link -->
        <a href="/login" class="login-link">Already have an account? Login</a>
        <!-- Alert for registration -->
        <div class="alert" id="register-alert"></div>
    </div>

    <script>

    </script>




</body>
</html>